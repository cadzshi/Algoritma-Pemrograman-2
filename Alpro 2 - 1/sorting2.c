#include<stdio.h>
/*
//bubble sort
int main()
{
    int tabint[5] = {34, 67, 23, 28, 98};
    int i, j, temp, tukar;

    do
    {
        //inisialisasi nilai tukar sebelum ada pertukaran diset false
        tukar = 0;
        //pengulangan dan memeriksa apakah ada pertukaran diset false
        for (i = 0; i < (5 - 1); i++)
        {
            //jika ada yang dipertukarkan
            if (tabint[i] > tabint[i + j])
            {
                //menukar posisi elemen
                temp = tabint[i];
                tabint[i] = tabint[i + j];
                tabint[i + j] = temp;
                tukar = 1;
            }
        }
        
    } while (tukar == 1);
    
    return 0;
}
*/

//quick sort (pivot pingggir)
void quicksortpivotpingggir(int l, int r)
{
    int  i, j, temp, tabint[5];
    i = l;
    j = r;
    do
    {
        while (tabint)
        {
            /* code */
        }
        
    } while (tabint[i] < tabint[r] && (i <= j));
    
}
