#include "header.h" //panggil header
int main()
{
    int a, b, c, n;                       // deklarasi variabel,a angka pertama, b angka kedua, c angka ketiga, n adalah jumlah keluarnya
    scanf("%d %d %d %d", &a, &b, &c, &n); // scan
    r(a, b, c, n);                        // panggil prosedur

    return 0;
}
